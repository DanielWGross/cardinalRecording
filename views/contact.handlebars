<div class="container">
  <div class="row">
    <div class="col-8 offset-2">
      <div class="card mt-4">
        <div class="card-body">
          <h5 class="card-title"> CONTACT US</h5>
          <p>
          <form action="/contact" {{this.body}} method="POST">
            <strong>Name</strong>:<input type="text" id="name"> {{this.body.name}}</input>
          </p>
          <p>
          <p>
            <strong>Phone</strong></p><input type="te" id="phone"> {{this.body.number}}</input>
          <p>
            <strong>Email:</strong></p><input type="email" id="email"> {{this.body.number}}</input>
          <p>
            <strong>Message:</strong> 
            <textarea type="text" id="message">{{this.body.message}}</textarea>
            </p>
          </form>
            <a href="/">← Back To Home</a>
        </div>
      </div>
    </div>
  </div>
</div>
 